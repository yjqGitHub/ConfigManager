@using JQ.Result.Page;
@model IPageResult<PubConfigGroupDto>

<div class="page-header">
    <nav>
        <span class="r">
            <a href="/PubConfigGroup/Add?environmentID=@(ViewBag.EnvironmentID?.ToString())&w=800&h=600" title="添加配置组" class="openDialog btn btn-primary radius">
                <i class="Hui-iconfont">&#xe600;</i> 添加配置组
            </a>
        </span>
    </nav>
</div>
<div class="page-container">
    @using (Html.BeginForm("Index", "PubConfigGroup", FormMethod.Post, new { @id = "SearchForm" }))
    {
        <div class="page-container-search">
            <div class="form-group">
                <label>AppId:</label>
                @Html.TextBox("AppId", "", new { @style = "width:150px", @placeholder = "输入AppId" })
            </div>
            <div class="form-group"><label>AppId:</label>@Html.TextBox("AppId", "", new { @style = "width:150px", @placeholder = "输入AppId" })</div>
            <div class="form-group"><label>AppId:</label>@Html.TextBox("AppId", "", new { @style = "width:150px", @placeholder = "输入AppId" })</div>
            <div class="form-group"><label>AppId:</label>@Html.TextBox("AppId", "", new { @style = "width:150px", @placeholder = "输入AppId" })</div>
            <div class="form-group"><label>AppId:</label>@Html.TextBox("AppId", "", new { @style = "width:150px", @placeholder = "输入AppId" })</div>
            <div class="form-group"><label>AppId:</label>@Html.TextBox("AppId", "", new { @style = "width:150px", @placeholder = "输入AppId" })</div>
            <div class="form-group"><label>AppId:</label>@Html.TextBox("AppId", "", new { @style = "width:150px", @placeholder = "输入AppId" })</div>
            <div class="form-group"><label>AppId:</label>@Html.TextBox("AppId", "", new { @style = "width:150px", @placeholder = "输入AppId" })</div>
            <div class="form-group"><label>AppId:</label>@Html.TextBox("AppId", "", new { @style = "width:150px", @placeholder = "输入AppId" })</div>
            <div class="form-group"><label>AppId:</label>@Html.TextBox("AppId", "", new { @style = "width:150px", @placeholder = "输入AppId" })</div>
            <div class="form-group"><label>AppId:</label>@Html.TextBox("AppId", "", new { @style = "width:150px", @placeholder = "输入AppId" })</div>
            <div class="form-group"><label>AppId:</label>@Html.TextBox("AppId", "", new { @style = "width:150px", @placeholder = "输入AppId" })</div>
            <div class="form-group"><label>AppId:</label>@Html.TextBox("AppId", "", new { @style = "width:150px", @placeholder = "输入AppId" })</div>
            <div class="form-group"><label>AppId:</label>@Html.TextBox("AppId", "", new { @style = "width:150px", @placeholder = "输入AppId" })</div>
            <div class="form-group"><label>AppId:</label>@Html.TextBox("AppId", "", new { @style = "width:150px", @placeholder = "输入AppId" })</div>
            <div class="form-group">
                <button type="submit" class="btn btn-success radius"><i class="Hui-iconfont"></i> 搜索</button>
            </div>
        </div>
        <div class="page-container-list">
            <table class="table table-border table-bordered table-bg table-hover" id="table-head-fixer">
                <thead>
                    <tr class="text-c">
                        <th width="25">
                            编号
                        </th>
                        <th>名字</th>
                        <th>编码</th>
                        <th>环境信息</th>
                        <th>是否启用</th>
                        <th>备注</th>
                        <th>最后修改时间</th>
                        <th>操作</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Count() > 0)
                {
                    int currentRow = 1;

                    foreach (var item in Model)
                    {
                            <tr class="text-c">
                                <td>@currentRow.ToString()</td>
                                <td>@item.FName</td>
                                <td>@item.FCode</td>
                                <td>@item.GetEnvironmentDesc()</td>
                                <td>@item.FIsEnabled</td>
                                <td>@item.FComment</td>
                                <td>@(item.FLastModifyTime.ToString("yyyy-MM-dd HH:mm:sss"))</td>
                                <td class="td-manage">
                                    <a href="/PubConfigGroup/Edit/@(item.FID.ToString())?w=700&h=400" title="编辑配置组" class="openDialog btn btn-primary size-S radius">编辑</a>
                                </td>
                            </tr>
                            currentRow++;
                        }
                    }
                    else
                    {
                        @Html.Raw("<tr><td style='color:red;text-align:center;' colspan='8'>暂无记录</td></tr>")
                    }
                </tbody>
            </table>
        </div>
        <div class="page-container-pager noselect">
            @Html.SelfPager(Model)
        </div>
    }

</div>
